using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Character_3D : MonoBehaviour
{
    // Start is called before the first frame update
    private void Awake()
    {
        /*Vector3 vec = new Vector3();

        float x, y;

        if (x == y) //float은 ==이 안됨 int만 가능 //도착
        {
        }

        Vector3.Distance(); 
        Vector3.Dot();
        Vector3.Cross();*/

        /*Quaternion que = transform.rotation; //회전은 vector가 아닌 이유. 짐벌락 현상, 행과 열 사이에 끼임현상 발생.
        Matrix4x4 mat = */
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
